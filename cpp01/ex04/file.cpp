#include "file.hpp"

//open filename 
//read from it
//open new file .replace
//put the content of the filename into newfile
//then start replacing s1 with s2 if it found it

int ft_open(std::string filename)
{
    std::ifstream filename;
    filename.
}